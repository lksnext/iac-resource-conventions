# Project dictionary — words that are correct but not covered by cspell's bundled
# dictionaries or the en/en-GB locales enabled in cspell.config.jsonc. Every entry below
# is grouped with the reason it is needed; extend this file only when a real word is
# flagged, not speculatively (see CONTRIBUTING.md#spell-checking).
#
# One word per line, lowercase unless the word is always capitalized (a proper noun).

# Organization / project identifiers
lksnext

# Convention Pack naming (specification/convention-packs/, README.md roadmap) — the
# planned `aws-controltower` Convention Pack slug names the AWS service as a single
# lowercase word, matching how it appears in package/slug identifiers.
controltower

# IaC / cloud domain terminology not in the bundled aws/k8s/terraform dictionaries
multitenancy

# AWS S3's own reserved bucket-name prefix (docs.aws.amazon.com bucket naming rules),
# cited verbatim in packages/catalog/src/aws/s3-bucket.ts
sthree

# General technical/software terms
transpilable
subcomponent
executability
unrepresentable

# Tool and package names referenced in docs/config (not covered by npm/node/git
# dictionaries)
nexe
lycheeverse
pipx

# Author of the license-checker-rseidelsohn devDependency (see
# CONTRIBUTING.md#dependency-license-compliance), referenced in its own package name
rseidelsohn

# Python environment terminology used in the Dev Container Dockerfile comments
venv
virtualenvs

# Git config keys referenced in .github/copilot-instructions.md
signingkey
gpgsign

# VS Code extension authors/publishers referenced in .vscode/extensions.json and
# .devcontainer/devcontainer.json
Anson
charliermarsh

# Unicode Default Case Conversion conformance test fixtures
# (packages/core/test/runtime/naming-evaluation.test.mjs) — real-world non-ASCII words
# demonstrating one-to-many case mappings (German ß -> "SS", Turkish İ -> "i" + U+0307),
# used as evidence that casing follows ECMAScript's Default Case Conversion algorithm,
# not the strictly one-to-one "Unicode simple case mapping".
straße
STRASSE
İstanbul
stanbul

# Milestone 3.3 catalog conformance review terminology
# (docs/architecture/resource-definition-catalog-conformance.md)
globality
executably

# Specification v1.2 (Executable Resource Constraints) — GitHub-style markdown anchor
# slugs referenced as link fragments in prose (for example
# specification/resource-definition.md#startend-constraints,
# #resourcetype-semantic-variants-specification-v12), derived mechanically from
# heading text ("Start/end constraints", "ResourceType semantic variants") and not
# themselves prose words.
startend
resourcetype

# Regular-expression dialect and policy-language names cited in the Specification
# v1.2 regex decision (specification/resource-definition.md#regex-decision) and its
# Non-Goals (specification/README.md#specification-v12-non-goals)
PCRE
Rego

# Milestone 4.2 Convention Pack Catalog terminology
# (docs/architecture/convention-pack-catalog.md,
# packages/catalog/src/internal/validate-convention-pack.ts) — "representability" is a
# real word (the quality of being representable), just not in the bundled dictionaries.
representability
# Markdown anchor slug for "Specification/runtime fidelity", derived mechanically from
# heading text (the "/" is dropped by GitHub's slug algorithm), not itself a prose word.
specificationruntime

# npm CLI flag name (`--userconfig`), used by scripts/smoke-test-github-packages.mjs to
# point at a disposable, untracked .npmrc for the post-publication GitHub Packages smoke
# test — not itself a prose word.
userconfig

# Azure CAF abbreviations and ARM resource-type path segments cited verbatim from
# official Microsoft Learn documentation (Azure portability slice)
vnet
snet
resourcegroups
virtualnetworks
pgsql
mydemoserver

# Azure region short names (Azure CAF abbreviations table), cited verbatim
eastus
westus
centralus
northeurope
uksouth
ukwest

# Azure Resource Manager naming-rules documentation terminology
# (packages/catalog/src/azure/resource-group.ts)
Titlecase

# Milestone 5 (Azure portability slice) scoping terminology
# (docs/architecture/resource-definition-catalog.md)
unmodeled


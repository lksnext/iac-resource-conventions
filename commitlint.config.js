// Commitlint configuration — enforces Conventional Commits (see CONTRIBUTING.md#commit-messages).
// Scopes are free-form (package/area names such as core, catalog, cli, specification,
// monorepo, devcontainer, github); no fixed scope list is enforced.
module.exports = {
  extends: ["@commitlint/config-conventional"],
};
